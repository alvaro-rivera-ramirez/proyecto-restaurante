/**
 * ------------------------------------------------------------------------
 * Code JS for component defined as desplegable
 * ------------------------------------------------------------------------
 */

//  const desplegables = document.querySelectorAll('.desplegable');

//  desplegables.forEach(desplegable => {
//      const select = desplegable.querySelector('.desplegable-toggle');
//      const caret = desplegable.querySelector('.caret');
//      const menu = desplegable.querySelector('.desplegable-menu');
//      const options = desplegable.querySelector('.desplegable-menu li');
 
//      select.addEventListener('click', () => {
//          caret.classList.toggle('caret-rotate');
//          menu.classList.toggle('menu-open');
//      });
 
//      options.forEach(option => {
//          option.addEventListener('click', () => {
//              caret.classList.remove('caret-rotate');
//              menu.classList.remove('menu-open');
//          });
//      });
//  });