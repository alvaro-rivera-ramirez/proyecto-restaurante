<script>
    let list_mesas=[1,2,3,4];
    var dato;
    for (let item of list_mesas){
        console.log(item)
    }
    let tipo_pedido="johan marcos";
    let _tipo="esternocleido";
    let name=1;
    let union={tipo_pedido,_tipo,name};
    list_mesas.push(union);
    console.log(list_mesas)
    window.onload =getprueba(46);
    function getprueba(id){
        id=[1,2,3]
        fetch(`/api/user/${id}`)
        .then( response => response.json() )
        .then( usuario =>{
            prod=usuario;
            console.log(usuario);
            for(let item of list_mesas){
        dato=1;
        for(let item of list_mesas){
            dato=2;
        }
    }
        })
    }
    
    console.log(dato)
</script>